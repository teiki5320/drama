import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color paperCream = Color(0xFFFBF7EF);
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF6B6B6B);
  static const Color accentOrange = Color(0xFFD97757);

  // iMessage-style bubbles
  static const Color smsBlue = Color(0xFF007AFF);
  static const Color smsGray = Color(0xFFE9E9EB);

  // Subtle dividers / cards
  static const Color cardBorder = Color(0x1A1A1A1A);
  static const Color cardBg = Color(0xFFFFFFFF);

  // Semantic
  static const Color positive = Color(0xFF2E7D32);
  static const Color negative = Color(0xFFC62828);
}
